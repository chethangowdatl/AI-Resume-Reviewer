PORT=5000

# Set your preferred API key below.
# If GROQ_API_KEY is present, it will be prioritized (Free & fast!).
# If none are set, the app will run 100% FREE using the built-in Local AI Analyzer.

GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
